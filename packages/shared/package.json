{
  "name": "@template-dev/shared",
  "version": "1.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "bunx swc src -d dist --strip-leading-paths --ignore '**/*.d.ts' --copy-files",
    "clean": "rm -rf dist",
    "lint": "echo 'No linting configured'",
    "test": "echo 'No tests configured'"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
